version: '3.8'

services:
  postal-web:
    build:
      context: .
      target: full
    image: ${POSTAL_IMAGE:-postal:latest}
    container_name: postal-web
    entrypoint: ["/docker-entrypoint.sh"]
    command: ["postal", "web-server"]
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - gckokk44ckskcs4s0wgc8kk0-Config:/config
      - gckokk44ckskcs4s0wgc8kk0-Postal Data:/opt/postal/data
    env_file:
      - .env
    environment:
      - POSTAL_CONFIG_FILE_PATH=/config/postal.yml
      - POSTAL_SIGNING_KEY_PATH=/config/signing.key
      - BIND_ADDRESS=0.0.0.0
      - PORT=5000
    depends_on:
      - postal-worker
    networks:
      - postal-network
      - coolify
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  postal-worker:
    build:
      context: .
      target: full
    image: ${POSTAL_IMAGE:-postal:latest}
    container_name: postal-worker
    entrypoint: ["/docker-entrypoint.sh"]
    command: ["postal", "worker"]
    restart: unless-stopped
    volumes:
      - gckokk44ckskcs4s0wgc8kk0-Config:/config
      - gckokk44ckskcs4s0wgc8kk0-Postal Data:/opt/postal/data
    env_file:
      - .env
    environment:
      - POSTAL_CONFIG_FILE_PATH=/config/postal.yml
      - POSTAL_SIGNING_KEY_PATH=/config/signing.key
      - HEALTH_SERVER_BIND_ADDRESS=0.0.0.0
    networks:
      - postal-network
      - coolify
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9090/health", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  gckokk44ckskcs4s0wgc8kk0-Config:
    external: true
    name: gckokk44ckskcs4s0wgc8kk0-Config
  gckokk44ckskcs4s0wgc8kk0-Postal Data:
    external: true
    name: gckokk44ckskcs4s0wgc8kk0-Postal Data

networks:
  postal-network:
    driver: bridge
  coolify:
    external: true
    name: coolify